#ifndef _OSDEFINE_H#define _OSDEFINE_H//////////////////////////////////////////////////////////////////////////#define QCHAR	char#define QWCHAR  wchar_t#define QBYTE	unsigned char#define QPBYTE  unsigned char * #define QINT	int#define QSHORT  short#define QLONG	long#define QUINT	unsigned int#define QDWORD	unsigned long #define QWORD	unsigned short#define QVOID	void#define QBOOL	bool#define QTRUE	1#define QFALSE	0#define QNULL   0#define FALSE   0#define TRUE    1#ifndef NULL#define NULL 0#endif#define INLINE inlinetypedef char			        QINT8;typedef short			        QINT16;typedef int                     QINT32;typedef unsigned char	        QUINT8;typedef unsigned short	        QUINT16;typedef unsigned int            QUINT32;    typedef unsigned long long		QUINT64;typedef long long				QINT64;#define LPCSTR		const QCHAR *#define LPSTR		QCHAR *#define LPCWSTR		const QWCHAR *#define LPWSTR		QWCHAR *#ifndef WIN32typedef const QCHAR * LPCTSTR;#endif#ifdef __SYMBIAN32__#define QLPTSTR16 TUint16 *#define QLPTSTR8  TUint8 *#endif //__SYMBIAN32__#define BPP16M 3#define BPP64K 2#if !defined(MIN)#define MIN(A,B)	((A) < (B) ? (A) : (B))#endif#if !defined(MAX)#define MAX(A,B)	((A) > (B) ? (A) : (B))#endif#ifndef  _RELEASE#define _RELEASE( x ) if( nil != (x)){ [(x) release] ; (x) = nil ; }#endif#ifndef _FORMATSIZE#define _FORMATSIZE(x)  if((x).width <=0 || (x).width >= NSUIntegerMax){(x).width=160;}#endif#if !defined(PRINT)#define PRINT					printf#endif#if !defined(ASSERT)#define ASSERT					assert#endif#if !defined(QASSERT)#define QASSERT					assert#endif// 判断是否高清屏#define isRetina ([UIScreen instancesRespondToSelector:@selector(scale)] ? (2 == [[UIScreen mainScreen] scale]) : NO)//从qqglobal移过来的宏定义ximilu#define HEADFACE_SIZE			50//46 // 这个是给外面用的，比如会话列表和聊天等看上去稀疏的界面 #define CORNER_RADIUS			5.0 // 圆角大小//////////////////////////////////////////////////////////////////////////#endif //_OSDEFINE_H